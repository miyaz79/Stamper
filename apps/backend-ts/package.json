{
  "name": "@stamper/backend-ts",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "lint": "eslint .",
    "dev": "echo 'use sam local start-api if needed'"
  },
  "devDependencies": {
    "@stamper/eslint-config": "*",
    "@types/aws-lambda": "^8.10.129",
    "@types/node": "^20.11.30",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@stamper/types": "*",
    "@aws-sdk/client-cognito-identity-provider": "^3.614.0",
    "jose": "^5.2.4",
    "zod": "^3.23.8"
  }
}
